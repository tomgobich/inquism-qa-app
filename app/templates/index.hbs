<h1>Welcome to the home page</h1>

<div class="row">
  <div class="col-md-8">
    {{#each model as |question|}}
      <div class="well">
        <h4>{{question.question}}</h4>
        <p>
          Tags: {{question.tags}}
        </p>
      </div>
    {{/each}}
  </div>
</div>